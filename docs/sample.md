# Sample Document

This is a placeholder document so the pipeline always has something to read.
Replace it with real `.txt`, `.md`, `.json`, `.csv`, `.html`, `.xml`, `.py`,
`.log`, `.pdf`, or `.docx` files.

## Notes

- Agent 1 reads everything in this folder and concatenates the text.
- Agent 2 performs an extensive analysis using only these docs by default
  and may call Exa at most 3 times if a fact is genuinely missing.